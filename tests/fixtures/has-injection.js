// Test fixture: Known injection violations
const query = "SELECT * FROM users WHERE id = " + req.params.id;
const q2 = `SELECT * FROM products WHERE name = '${input}'`;
const { exec } = require('child_process');
exec('ls ' + userInput, callback);
